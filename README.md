There are you can find somebody C lang code than work with graphics has rendering uses CPU.
This is simply one way to output graphics primitives. It might come in handy, but otherwise, it's just for now.
